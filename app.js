// const { sum } = require("./calculate/sum");

// const { multiple } = require("./calculate/multiple");

const { sum, multiple } = require("./calculate");

console.log(sum(10, 20));

console.log(multiple(10, 20));

console.log("Entry file for Node");
